// Jobs List Showcase for the WorkSpace

import React from "react";

type Props = {};
// Right now it's my test Yard
const page = (props: Props) => {
  return (
    <div className="w-full h-screen flex items-center justify-center">
      <div>Test Yard</div>
    </div>
  );
};

export default page;
