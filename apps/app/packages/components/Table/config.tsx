import React from "react";

type Props = {
  selectedItem: any | null;
  updateItem: (newData: any) => void;
};

const TableConfigPanel = ({ selectedItem, updateItem }: Props) => {
  return <div>TableConfigPanel</div>;
};

export default TableConfigPanel;
