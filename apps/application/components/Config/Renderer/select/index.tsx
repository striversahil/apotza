import React from "react";
import Select_Base from "../../base/select";

type Props = {
  location: Array<string>;
  initialvalue: string;
};

const Select = ({ location, initialvalue }: Props) => {
  return <div>{/* <Select_Base /> */}</div>;
};

export default Select;
