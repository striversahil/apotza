import React from "react";

type Props = {
  location: Array<string>;
  initialvalue: string;
};

export const Icon = (props: Props) => {
  return <div className="float-end">Icon</div>;
};

export default Icon;
